CREATE TABLE realtimelogging (timestamp date, appdomain int, sku text, simple text, customer_id text, session text, action text, quantity text, price text, order_id text, permcookie text, abtest text, referrer text);

